{
    "name": "@pixelrpg/engine-excalibur",
    "version": "0.0.1",
    "description": "Excalibur engine for the pixelrpg project",
    "main": "dist/index.html",
    "type": "module",
    "private": true,
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "clear": "rm -rf dist",
        "start": "yarn clear && yarn build && vite preview",
        "build:esbuild": "esbuilder --outdir ./dist",
        "build:vite": "vite build",
        "build:assets": "mkdir -p ./dist && cp -r ./src/assets ./dist",
        "build": "yarn build:assets && yarn build:vite",
        "check": "tsc --noEmit"
    },
    "author": "Pascal Garber <pascal@artandcode.studio>",
    "license": "MIT",
    "devDependencies": {
        "@pixelrpg/esbuild": "workspace:^",
        "@rollup/pluginutils": "^5.1.4",
        "typescript": "^5.7.3",
        "vite": "^6.1.1"
    },
    "dependencies": {
        "@excaliburjs/dev-tools": "^0.28.0",
        "@excaliburjs/plugin-tiled": "workspace:^",
        "@pixelrpg/common": "workspace:^",
        "@pixelrpg/map-format-core": "workspace:^",
        "@pixelrpg/map-format-excalibur": "workspace:^",
        "@pixelrpg/messages-webview": "workspace:^",
        "excalibur": "workspace:^",
        "vite-plugin-glsl": "^1.3.1"
    }
}
