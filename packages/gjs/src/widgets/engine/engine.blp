using Gtk 4.0;
using Adw 1;

template $Engine : Adw.Bin {
  vexpand: true;
  hexpand: true;
  visible: true;

  Gtk.Box canvasContainer {
    vexpand: true;
    hexpand: true;
    orientation: vertical;
  }
}
