/**
 * GJS-specific type definitions to enhance compatibility with web standards.
 * These definitions supplement our polyfill implementations to provide
 * proper type checking in TypeScript.
 */

import "@girs/gjs";
import "@girs/gjs/dom";

// Prevent TypeScript from treating this as a module
export { };
